PORT=5000
# Default DB path is server/conveyor.db, independent of the launch directory.
# DB_PATH=C:/absolute/path/to/conveyor.db

# Physical motor actuation is disabled by default. No hardware actuation is
# needed for the demo. Enabling this requires independent hardware safety work.
ALLOW_MOTOR_CONTROL=false
ALLOW_HISTORY_DELETE=false
